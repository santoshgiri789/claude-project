# Requirement Traceability Matrix

Generated by scanning `REQ-SHORT-NNN` comments in `src/main` and the scenario/REQ comments
in `src/test`. Maps each requirement in [`specs/url-shortener.yaml`](../specs/url-shortener.yaml)
to the code that implements it and the test that exercises it.

| Requirement ID | Requirement (short) | Code File | Test File / Method | Status |
|----------------|---------------------|-----------|--------------------|--------|
| REQ-SHORT-001 | Shorten URL → return short code | `web/UrlController.java`, `service/UrlServiceImpl.java`, `dto/CreateUrlRequest.java`, `dto/UrlResponse.java` | `UrlShortenerIntegrationTest#scn001_shortenValidUrl_returns201WithShortCode` | Pending\* |
| REQ-SHORT-002 | Unique, URL-safe short code | `service/ShortCodeGenerator.java`, `entity/Url.java`, `repository/UrlRepository.java` | `…#scn001_…` (asserts short_code present); `…#scn006_…` (uniqueness via no-dup) | Pending\* (weak — uniqueness not asserted directly) |
| REQ-SHORT-003 | 302 redirect to original URL | `web/RedirectController.java`, `service/UrlServiceImpl.java` | `…#scn002_redirect_returns302ToOriginalUrl` | Pending\* |
| REQ-SHORT-004 | Unknown short code → 404 | `exception/NotFoundException.java`, `web/GlobalExceptionHandler.java`, `repository/UrlRepository.java` | `…#scn004_unknownShortCode_returns404`; `…#analyticsForUnknownCode_returns404` | Pending\* |
| REQ-SHORT-005 | Track click count / last accessed / referrer | `entity/ClickEvent.java`, `service/UrlServiceImpl.java`, `repository/ClickEventRepository.java` | `…#scn002_…` (click recorded); `…#scn008_analytics_returnsClickCountAndReferrers` | Pending\* |
| REQ-SHORT-006 | Analytics endpoint | `web/UrlController.java`, `dto/AnalyticsResponse.java`, `repository/ClickEventRepository.java` | `…#scn008_…`; `…#analyticsForUnknownCode_returns404` | Pending\* |
| REQ-SHORT-007 | Optional expiry → 410 Gone | `exception/GoneException.java`, `entity/Url.java` (`expiresAt`/`isExpired`), `entity/UrlStatus.java`, `service/UrlServiceImpl.java` | `…#scn005_expiredUrl_returns410` | Pending\* |
| REQ-SHORT-008 | Reject invalid / non-http(s) URLs | `service/UrlValidator.java`, `web/GlobalExceptionHandler.java` | `…#scn003_invalidUrl_returns400`; `…#missingUrlField_returns400`; `…#nonHttpScheme_returns400` | Pending\* |
| REQ-SHORT-009 | Duplicate URL → existing code | `service/UrlServiceImpl.java` (`findByOriginalUrl`) | `…#scn006_duplicateUrl_returnsExistingCodeWith200` | Pending\* |
| REQ-SHORT-010 | Block malicious / SSRF / internal hosts | `service/UrlValidator.java` | `…#scn007_ssrfUrl_returns400`; `…#localhostUrl_isBlocked_returns400`; `…#nonHttpScheme_returns400` | Pending\* |
| REQ-SHORT-011 | Structured JSON error responses | `dto/ErrorResponse.java`, `web/GlobalExceptionHandler.java` | `…#scn003_…` (asserts timestamp/status/message); `…#scn004_…` (asserts status) | Pending\* |
| REQ-SHORT-012 | No open redirect (only stored URLs) | `service/UrlValidator.java`, `service/UrlServiceImpl.java` (redirect-time scheme guard) | `…#scn007_ssrfUrl_returns400` | Pending\* (indirect — no dedicated open-redirect test) |

_All test methods are in `src/test/java/com/business/urlshortener/UrlShortenerIntegrationTest.java` (abbreviated `…` above)._

## Gaps & flags

**Requirements with no test:** none — every REQ-SHORT-001…012 has at least one mapped test.

**Requirements with weak/indirect coverage (have a test, but worth strengthening):**
- **REQ-SHORT-002** — tests assert a short code is returned and that duplicates don't create
  new rows, but uniqueness/URL-safe-charset is not asserted directly. Suggest a unit test on
  `ShortCodeGenerator` (charset + collision retry).
- **REQ-SHORT-012** — covered only transitively via the SSRF test (malicious URL never stored).
  Suggest an explicit test that a stored `http(s)` target is the only thing ever redirected to.

**Tests not tied to any requirement:**
- `UrlShortenerApplicationTests#contextLoads` — infrastructure smoke test (Spring context /
  JPA wiring). Intentionally not mapped to a REQ; kept as a sanity check.

**Requirements not carrying a REQ comment in code:** none. All twelve appear in `src/main`
`Implements:` headers.

## Summary

**12 of 12 requirements have full code+test coverage** (every REQ-SHORT-001…012 is implemented
in `src/main` and exercised by at least one test in `src/test`).

\* **Status = Pending:** the mapping is complete, but `mvn test` has **not been executed** in
this environment (no Maven / Spring-JPA jars available offline), so pass/fail is unverified.
Re-run `mvn test` on a machine with the toolchain and update the Status column to pass/fail
from the actual results.